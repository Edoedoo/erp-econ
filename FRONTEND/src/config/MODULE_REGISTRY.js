import { INVENTORY_MENU } from "../modules/inventory/config/INVENTORY_MENU";
import { ACCOUNTING_MENU } from "../modules/accounting/config/ACCOUNTING_MENU";

export const MODULE_REGISTRY = {
    accounting: ACCOUNTING_MENU,
    inventory: INVENTORY_MENU,
}